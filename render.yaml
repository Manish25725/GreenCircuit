services:
  # Backend Service
  - type: web
    name: ecocycle-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: PORT
        value: 3001
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
      - key: ADMIN_KEY
        sync: false

  # Frontend Service (Static Site)
  - type: web
    name: ecocycle-frontend
    env: static
    plan: free
    buildCommand: npm install && NODE_ENV=production npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://e-waste-7ios.onrender.com/api
      - key: NODE_ENV
        value: production
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
